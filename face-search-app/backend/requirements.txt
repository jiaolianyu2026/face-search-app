# Core dependencies for face recognition search application
# Using dlib-bin (precompiled) instead of dlib for easier installation
dlib-bin>=19.7.0
face_recognition>=1.3.0
opencv-python>=4.8.0
numpy>=1.26.0
Pillow>=10.0.0
Flask>=3.0.0
Flask-CORS>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0

# Testing dependencies
pytest>=9.0.0
hypothesis>=6.0.0

# Agent 层依赖（LangChain 框架 + 工具支持）
# LangChain 核心框架，用于构建 ReAct Agent 和工具调用
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0

# 环境变量管理（加载 .env 中的 API Key）
python-dotenv>=1.0.0

# HTTP 客户端（Agent 调用后端 API 时使用）
requests>=2.31.0
